"use client";

import { LoginScreen } from "@/components/auth/LoginScreen";

/** Platform sign-in. A workspace on its own domain signs in at that root. */
export default function LoginPage() {
  return <LoginScreen />;
}
